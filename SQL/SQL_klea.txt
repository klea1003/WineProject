시퀀스 생성 
CREATE SEQUENCE SEQ_WINE; 

와인테이블 생성 
CREATE TABLE TBL_WINE_TEST (
    WINE_NUM NUMBER(20) NOT NULL,
    WINE_TITLE VARCHAR2(50),
    WINENERY VARCHAR2(50),
    WINE_GRAPES VARCHAR2(100),
    WINE_REGIONS VARCHAR2(50),
    WINE_ALLERGENS VARCHAR2(50),
    WINETYPE VARCHAR2(50),
    WINE_REGION VARCHAR2(50),
    WINE_COUNTRY VARCHAR2(50),
    WINE_PRICE NUMBER(20),
    WINE_ALCOHOL FLOAT,
    WINESTYLE VARCHAR2(50),
    WINE_IMAGENAME VARCHAR2(50),
    PRIMARY KEY(WINE_NUM)
);

COMMIT;


와인 데이터 추가(페이징처리)  
	INSERT INTO TBL_WINE_TEST(WNO,TITLE,WINENERY,GRAPES,REGIONS,ALLERGENS,WINETYPE,
	REGION,COUNTRY,PRICE,ALCOHOL,WINESTYLE,IMAGENAME)
	(SELECT SEQ_WINE.NEXTVAL,TITLE,WINENERY,GRAPES,REGIONS,ALLERGENS,WINETYPE
	REGION,COUNTRY,PRICE,ALCOHOL,WINESTYLE,IMAGENAME FROM TBL_WINE_TEST);  

COMMIT;