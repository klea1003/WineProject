<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.wine.mapper.UserMapper">

	<resultMap type="org.wine.domain.UserVO" id="userInfo">
		<result property="userNum" column="USER_NUM" />
		<result property="userId" column="USER_ID" />
		<result property="userNickName" column="USER_NICKNAME" />
		<result property="userEmail" column="USER_EMAIL" />
		<result property="userPassword" column="USER_PASSWORD" />
		<result property="userPhoneNum" column="USER_PHONENUM" />
		<result property="userRealName" column="USER_REALNAME" />
		<result property="userBirthDay" column="USER_BIRTHDAY" />
	</resultMap>

	<resultMap type="org.wine.domain.CartVO" id="cartList">
		<result property="cartNum" column="CART_NUM" />
		<result property="userNum" column="USER_NUM" />
		<result property="sellerNum" column="SELLER_NUM" />
		<result property="wineNum" column="WINE_NUM" />
		<result property="cartQty" column="CART_QTY" />
		<result property="userRealName" column="USER_REALNAME" />
		<result property="sellerId" column="SELLER_ID" />
		<result property="wineTitle" column="WINE_TITLE" />
		<result property="winePrice" column="WINE_PRICE" />
		<result property="totalPrice" column="TOTAL_PRICE" />
		<result property="wineType" column="WINETYPE"/>
		<result property="wineCountry" column="WINE_COUNTRY"/>
		<result property="wineImageName" column="WINE_IMAGENAME" />
	</resultMap>

	<select id="getList" resultMap="userInfo">
		select*from tbl_user where
		USER_NUM >0
	</select>

	<insert id="join">
		insert into TBL_USER(USER_NUM, USER_ID,
		USER_NICKNAME,
		USER_EMAIL,USER_PASSWORD,USER_PHONENUM,USER_REALNAME,USER_BIRTHDAY)
		values(SEQ_USER_TEST.nextval,#{userId},#{userNickName},#{userEmail},#{userPassword},#{userPhoneNum},#{userRealName},to_char(to_date(#{userBirthDay})))
	</insert>

	<insert id="joinSelectKey">
		<selectKey keyProperty="userNum" order="BEFORE"
			resultType="long">
			select SEQ_USER_TEST.nextval from dual
		</selectKey>
		insert into TBL_USER(USER_NUM, USER_ID, USER_NICKNAME,
		USER_EMAIL,USER_PASSWORD,USER_PHONENUM,USER_REALNAME,USER_BIRTHDAY)
		values(#{userNum},#{userId},#{userNickName},#{userEmail},#{userPassword},#{userPhoneNum},#{userRealName},to_char(to_date(#{userBirthDay})))
	</insert>

	<select id="read" resultMap="userInfo">
		select * from TBL_USER where USER_NUM=#{userNum}
	</select>

	<update id="update">
		update TBL_USER set USER_EMAIL=#{userEmail},
		USER_PASSWORD=#{userPassword}, USER_PHONENUM=#{userPhoneNum},
		USER_BIRTHDAY = to_char(to_date(#{userBirthDay}))
		where
		USER_NUM=#{userNum}
	</update>

	<select id="idCheck" resultType="int">
		select count(*) from TBL_USER
		where USER_ID = #{userId}
	</select>

	<select id="nicknameCheck" resultType="int">
		select count(*) from
		TBL_USER where USER_NICKNAME = #{userNickName}
	</select>

	<select id='userLogin' resultMap="userInfo">
		SELECT
		USER_NUM,USER_ID,USER_NICKNAME,USER_EMAIL,USER_PHONENUM,USER_REALNAME,USER_BIRTHDAY
		FROM TBL_USER
		WHERE USER_ID=#{userId} and USER_PASSWORD=#{userPassword}
	</select>

	<!-- 장바구니 목록 -->
	<select id="getwishList" resultMap="cartList">
		select CART_NUM, U.USER_NUM, S.SELLER_NUM, SELLER_ID,W.WINETYPE,W.WINE_COUNTRY,
		W.WINE_NUM, WINE_TITLE, WINE_PRICE, WINE_IMAGENAME, CART_QTY,
		(WINE_PRICE*CART_QTY)TOTAL_PRICE
		from
		TBL_USER U, TBL_CART_TEST C, TBL_SELLER_TEST S, TBL_WINE_TEST W
		where
		U.USER_NUM = C.USER_NUM and C.WINE_NUM = W.WINE_NUM and C.SELLER_NUM =
		S.SELLER_NUM
		and C.USER_NUM = #{userNum} order by CART_NUM
	</select>
</mapper>
 